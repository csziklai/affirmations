import { StyleSheet } from 'react-native';

export const typography = StyleSheet.create({
    header: {
        fontSize: 24,
        fontFamily: 'LoraBold',
        fontWeight: 'bold',
        color: '#000',
        marginLeft: 20,
        marginBottom: 20,
    },
});